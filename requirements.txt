-e ./vendor/sam2
fastapi
uvicorn[standard]
python-multipart
pydantic
numpy
opencv-python
imageio-ffmpeg
decord
huggingface_hub
gdown

# 物体検出（COCO Mask R-CNN）。pip での直接インストールは git+ URL 経由。
# Detectron2 の setup.py は import 時に torch を要求するため、build isolation を切る必要がある
# （torch は本 requirements.txt の他依存で既に入っている前提）:
#   pip install --upgrade pip setuptools wheel ninja
#   pip install --no-build-isolation 'git+https://github.com/facebookresearch/detectron2.git'

# Casper sidecar 用（gen-omnimatte-public 依存。gradio / tensorboard / ray /
# flash_attn / xfuser / comfy / hpsv2 / datasets 等の sidecar API で使わない依存は
# 意図的に除外。albumentations は dataset_image_video.py が utils 経由で常時 import
# されるため必須）
diffusers>=0.30.1
# transformers 5.x は torch >= 2.7 を要求するため、torch 2.5.1 固定と衝突する。
# 4.x 系の最新を使う。
transformers>=4.46.2,<5
accelerate>=0.25.0
ml_collections
absl-py
omegaconf
einops
safetensors
loguru
mediapy
imageio[ffmpeg]
imageio[pyav]
torchdiffeq
torchsde
SentencePiece
protobuf
ftfy
func_timeout
kornia
timm
scikit-image
scipy
matplotlib
tomesd
beautifulsoup4
requests
albumentations
