# torch / torchvision / torchaudio はここに含めない。
# ユーザが自分の CUDA 環境に合わせて先に install する。推奨バージョンは README 参照。
# 本ファイルの各パッケージは major-cap だけ付けて minor/patch は浮動。
# 推奨 torch (現時点 2.5.x) から大きく離れなければ数年単位で動く想定。

# ---------- Web framework / API ----------
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
python-multipart>=0.0.9,<1
pydantic>=2.0,<3

# ---------- Numerical / Image / Video ----------
numpy>=1.24,<3
opencv-python>=4.5,<5
imageio-ffmpeg>=0.4,<1
decord>=0.6,<1
mediapy>=1.2,<2
imageio[ffmpeg]>=2.30,<3
imageio[pyav]>=2.30,<3
scikit-image>=0.21,<1
scipy>=1.10,<2
matplotlib>=3.7,<4
kornia>=0.7,<1
albumentations>=1.3,<3

# ---------- HuggingFace / Models / Downloading ----------
huggingface_hub>=0.20,<1
hf_transfer>=0.1,<1
gdown>=5.0,<6

# ---------- Diffusion / Casper deps ----------
# transformers 5.x は torch >= 2.7 を要求するため、推奨 torch 2.5.x と衝突する。
# 4.x 系に閉じる。
diffusers>=0.30,<1
transformers>=4.46.2,<5
accelerate>=0.25,<2
safetensors>=0.4,<1
einops>=0.7,<1
ml_collections>=0.1,<1
absl-py>=1.0,<3
omegaconf>=2.3,<3
torchdiffeq>=0.2,<1
torchsde>=0.2,<1
SentencePiece>=0.2,<1
timm>=0.9,<2
tomesd>=0.1,<1

# ---------- Misc ----------
loguru>=0.7,<1
protobuf>=4.0,<7
ftfy>=6.0,<7
func_timeout>=4.0,<5
beautifulsoup4>=4.10,<5
requests>=2.28,<3

# ---------- detectron2 が依存する追加パッケージ ----------
# detectron2 は requirements-detectron2.txt で --no-deps install するため、
# その transitive deps を本ファイルで明示的に確保する（iopath cap を逃れるため）。
# matplotlib / omegaconf / Pillow / tqdm は他経由で既に解決済み。
pycocotools>=2.0.2,<3
fvcore>=0.1.5,<0.2
iopath>=0.1.10,<1
yacs>=0.1.8,<1
hydra-core>=1.1,<2
cloudpickle>=2.0,<4
tensorboard>=2.10,<3
termcolor>=2.0,<4
tabulate>=0.9,<1

# ---------- SAM2 (commit pin) ----------
# setup.py が import 時に torch を要求するため、本ファイル全体を
# `pip install --no-build-isolation -r requirements.txt` で入れる必要がある
# （outer env の torch を借用してビルドする）。詳細は README 参照。
# 公式 SAM2 のディストリビューション名は `sam-2`（ハイフン）。import 名 `sam2` とは別物。
sam-2 @ git+https://github.com/facebookresearch/sam2.git@2b90b9f5ceec907a1c18123530e92e794ad901a4

# detectron2 は別ファイル (requirements-detectron2.txt) で --no-deps install する。
# 理由: 上流が `iopath>=0.1.7,<0.1.10` で cap しているが、sam-2 が `iopath>=0.1.10` を
# 要求し、conflict する。iopath 0.1.10 は detectron2 でも runtime 動作するので、
# --no-deps で iopath 制約を回避し、必要な deps は上の追加パッケージで賄う。
