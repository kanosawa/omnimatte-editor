# Frontend

Electron + React + Pixi. Tested on Windows; macOS and Linux may also work.

## Setup

```powershell
cd frontend
npm install
```

## Run

When the backend runs on a remote GPU server (via SSH tunnel):

```powershell
npm run dev:remote -- <user>@<host>:<ssh_port>
```
